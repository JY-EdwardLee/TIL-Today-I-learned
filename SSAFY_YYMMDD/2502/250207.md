# List2

### 2차원 배열
- 1차원 List를 묶어놓은 List \
※ 배열은 python에서 List임

- 2차원 배열의 선언
> - 세로길이(행의 개수), 가로길이(열의 개수)를 필요로 함
> - Python에서는 데이터 초기화를 통해 변수선언과 초기화가 가능함

- 입력을 2차원 배열에 저장하기
>- 리스트 형식으로\
> N = int(input)\
> arr = [list(map(int, input.split)) for _ in range(N)]

>- int 형식으로\
> N = int(input)\
> arr= [list(map(int, input.split)) for _ in range(N)]

- 0으로 채워진 3x4 배열 만들기
> arr = [[0]*4 for _ in range(3)] \
> ※ 꽤 자주 만들게 될 것

  - 배열 순회
      - n x m 배열의 n*개의 모든 원소를 빠짐없이 조사하는 법
  - 행 우선 순회
    ``` python
    for i in range(n):
      for j in range(m):
        f(array[i][i])
    ```
  - N x M 배여르이 크기오 저장된 값이 주어질 때 합을 구하는 법
    ``` python
    #N = 행
    #M = 열
    s = 0
    for i in range(N):
      for j in range(M):
        s += arr[i][j]
    ```
  
  - 열 우선 순회
    ``` python
    # i 행의 좌표
    # j 열의 좌표
    for j in range(m):
      for i in range(n):
        f(array[i][j])
    ```
  
  - 지그재그 순회
    ```python
      for i in range(n):
        for j in range(m):
          f(array[i][j + (m-1-2*j)*(i%2)])
    ```
### 델타를 이용한 탐색
 - 기본 탐색법 
    ```python
    di[] = [0, 1, 0, -1]
    dj[] = [1, 0, -1, 0]

    for k: 0 -> 3
      ni = i + dj[k]
      nj = j + dj[k]
    ```
  - 또 다른 방법
    ```python
      for i in range(N):
        for j in range(N):
          for di, dj in [0,1],[1,0],[0,-1],[-1,0]:
    ```

  - 델타 응용
      - NxN 배열에서 각 원소를 중심으로, 상하좌우 k칸의 합계 중 최대값 (k=2) 
    ```python
    max_v = 0
    for i inr range(N):
      for j in range(N):
        s = arr[i][j]
        for di, dj in [0,1],[1,0],[0,-1],[-1,0]:
          for c in range(1,k+1):
            ni, nj = i+di*c, j+dj*c
            if 0 <= ni < N and 0 <= nj < M:
              s += arr[ni][nj]
        if max_v < s:
          max_v = s
    ```

  - 전치 행렬
      - len을 사용해 몇 개의 행과 열이 있는지 점검
      - i > j를 통해 반으로 나눔
      ```python
      for i in range(3):
        for j in range(3):
          if i < j: # 두 번 바뀌어서 원래자로리 돌아가는 것을 예방
            arr[i][j], arr[j][i] = arr[j][i], arr[i][j]
      ```
  - [참고]i, j의 크기에 따라 접근하는 원소 비교
    - i < j = 오른쪽 위
    - i > j = 왼쪽 아래
    - i == j = 45도
      - 접근할 때는 arr[i][i]
    - N-1-i ==j = 135도
      - 접근할 때는 arr[i][N-1-i]

### 부분집합 합(Subset Sum) 문제
- 유한 개의 정수로 이루어진 집합이 이씅ㄹ 대, 이 집합의 부분집합 중에서 그 집합의 원소를 모두 더한 값이 0이 되는 경우가 있는지 알아내는 문제\
  - e.g. [-7,-3,-2,5,8]은 [-3,-2,5]가 합이 0이 되는 부분집합이다.

- 접근법
  - arr[i]가 부분집합에 포함되지 않으면 bit[i] ==0
  - arr[i]가 부분집합에 포함되면 bit[i] == 1
    ```python
    bit = [0, 0, 0, 0]
    for i in range(2):
      bit[0] = i
      for j in range(2):
        bit[1] = j
        for k in range(2):
          bit[2] = k
          for l in range(2):
            bit[3] = l
            print_subset(bit)
    ```
- 비트 연산자
  - 비트란? 

---
### 보강
- 전치 list(map(list,zip(matrix)))
  - 전\
  1 2 3\
  4 5 6\
  7 8 9
  - 치\
  1 4 7\
  2 5 8\
  3 6 9
- 반시계방향
  1) 전치 : i, j = j, i
  2) 행 뒤집기 : [::-1]
- 시계방향
  1) 행 뒤집기 : [::-1]
  2) 전치 : i, j = j, i
- 180도 돌리기
  = 시계방향 두번 적용

---
