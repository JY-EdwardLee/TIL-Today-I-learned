<aside>
💡

**과정 소개**

- 학습 내용
    
    : 자료구조와 알고리즘
    
- 학습 목표
    - 논리적 사고력 향상
    - 문제 해결 능력 향상
        
        ⇒ 최종 목표는 연습 문제가 아닌 “현실 세계 문제”
        
- 학습 도구
    - Python (Pypy) + PyCharm

- APS(Algorithm Problem Solving) 기본 학습
    - 입출력을 제외한 내장함수 사용하지 않기
    - 기본적인 내장함수의 동작원리 이해
</aside>

### 알고리즘

- 알고리즘 : 유한한 단계를 통해 **문제를 해결하기 위한 절차나 방법**이다. 주로 컴퓨터용어로 쓰이며, 컴퓨터가 어떤 일을 수행하기 위한 단계적 방법을 말한다.
- 간단하게 다시 말하면 어떠한 문제를 해결하기 위한 절차라고 볼 수 있다.
- 알고리즘을 표현하는 방법
    - 의사코드(슈도코드, Pseudocode)
    - 순서도
- 목표 : 보다 좋은 알고리즘을 이해하고 활용하는 것
    - 무엇이 좋은 알고리즘인가?
        1. **★ 정확성 : 얼마나 정확하게 동작하는가**
        2. 작업량
        3. 메모리 사용량
        4. 단순성
        5. 최적성
- 주어진 문제를 해결하기 위해 여러 개의 다양한 알고리즘이 가능
    
    **⇒ 어떤 알고리즘을 사용해야 하는가?**
    
- 알고리즘의 성능 분석 (다음 단계)
    - 많은 문제에서 성능 분석의 기준으로 알고리즘의 작업량을 비교한다.
- 작업량 **표현법 ⇒ 시간복잡도
    - 시간복잡도
        - 실제 걸리는 시간을 측정
        - 실행되는 명령문의 개수를 계산
        - 최악의 상황일 때의 성능 비교로 알고리즘의 시간 복잡도를 비교
    - 빅-오 표기법
        - 시간 복잡도 함수 중에서 가장 큰 영향력을 주는 n에 대한 항만 표시

### 배열 (자료구조 중 하나)

- 일정한 자료형의 변수들을 하나의 이름으로 열거하여 사용하는 자료구조
- 필요성
    - 프로그램 내에서 여러개의 변수가 필요할 때, 일일이 다른 변수명을 이용하여 자료에 접근하는 것은 비효율적
    - 하나의 선언을 통해 둘 이상의 변수 선언 가능
    - **단순 변수 선언이 아닌, 다수의 변수로 하기 힘든 작업을 배열을 활용해 쉽게 할 수 있음**
- 1차원 배열의 선언
    - 프로그램에서 사용할 배열의 이름이 필요함
        - e.g. arr = [0]*10 → 10칸짜리 일차원 배열
        
        ※ 그림을 그려 버릇 들어야 함
        
        ![image.png](attachment:300c1811-2554-4601-8987-e388a8f241bf:image.png)
        
- 1차원 배열의 접근
    - arr[0] = 10
    - arr[idx] = 20
    - 인덱스까지 가져다 놓으면 변수와 같은 역할
- 입력받은 정수를 1차원 배열에 저장하는 방법
    
    e.g.
    
    첫 줄에 양수의 개수 N이 주어진다.
    
    다음 줄에 빈칸으로 구분된 N개의 양수 ***Ai**가 주어진다 (*Ai : 자료형 결정)*
    
    ```python
    N = int(input())
    arr = list(map(int,input().split()))
    ```
    
    ※ 매 번 정성껏 타이핑을 해야 잊어먹지 않는다.
    
- 배열 원소의 합 s 계산하기
    
    ![image.png](attachment:40909dc6-c837-4228-9325-5aa1f919e744:image.png)
    
- 배열 원소 중 최댓값 max_v 찾기
    
    ![image.png](attachment:4150eb48-8833-4b58-ace0-699c9ea380ac:image.png)
    
    - 기왕이면 아래처럼 함수를 만들기
    
    ```python
    def my_max(arr,N)
    	return max_V
    ```
    
- 최댓값이 여러 개인 경우 첫번째 인덱스 찾
    
    ![image.png](attachment:b290778b-1ae0-4c24-8d82-1bf989861d73:image.png)
    
    - 맥스 인덱스만 있어도 value 값을 비교할 수 있기 때문에, max_v를 따로 갱신할 필요 없음
- 최댓값이 여러 개인 경우 마지막 인덱스 찾기
    
    ![image.png](attachment:f83a8b80-7eba-4029-a05e-798df7e8c43e:image.png)
    
- 찾는 이 배열에 있으면 해당 원소의 인덱스, 없으면 -1을 idx에 넣기
    
    idx = -1	# 찾는 값이 없다고 가정
    for i in range(N):
    if arr[i] == V:	# arr[i]가 찾는 값이면
    idx = i	# 인덱스 저장
    break	# for i
    
    ---
    

### 연습문제

1. SWEA > 4828
    
    N개의 양의 정수에서 가장 큰 수와 가장 작은 수의 차이를 출력하라.
    
    [입력]
    
    첫 줄에 테스트 케이스의 수 T가 주어진다. ( 1 ≤ T ≤ 50 )
    
    각 케이스의 첫 줄에 양수의 개수 N이 주어진다. ( 5 ≤ N ≤ 1000 )
    
    다음 줄에 N개의 양수 ai가 주어진다. ( 1 ≤ ai ≤ 1000000 )
    
    [출력]
    
    각 줄마다 "#T" (T는 테스트 케이스 번호)를 출력한 뒤, 빈칸에 이어 답을 출력한다.
    
2. 배열 활용 예제 : Gravity
    - 상자들이 쌓여있는 방이 있다. 방이 오른쪽으로 90도 회전하여 상자들이 중력의 영향을 받아 낙하한다고 할 때, 낙차가 가장 큰 상자를 구하여 그 낙차를 리턴 하는 프로그램을 작성하시오.
    - 중력은 회전이 완료된 후 적용된다.
    - 상자들은 모두 한쪽 벽면에 붙어진 상태로 쌓여 2차원의 형태를 이루며 벽에서 떨어져서 쌓인 상자는 없다.
    - 상자의 가로, 세로 길이는 각각 1이다.
    - 방의 가로길이는 1000이며, 세로 길이는 항상 100이다.
    - 즉, 상자는 최소 0, 최대 100 높이로 쌓을 수 있다.
    - 상자가 놓인 가로 칸의 수 N, 다음 줄에 각 칸의 상자 높이가 주어진다.
    - 아래 예) 총 26개의 상자가 회전 후, 오른쪽 방 그림의 상태가 된다. A 상자의 낙차가 7로 가장 크므로 7을 리턴하면 된다.
    - 회전 결과, B 상자의 낙차는 6, C 상자의 낙차는 1이다.
    
    입력 예시
    9
    7 4 2 0 0 6 0 7 0
    

---

## 정렬

- 2개 이상의 자료를 특정 기준에 의 작은 값부터 큰 값, 혹은 그 반대의 순서대로 재배열하는 것
- 키 : 자료를 정렬하는 기준이 되는 특정 값
    - 정렬 종류
        - ★ **버블 정렬**
        - 카운팅 정렬
        - 선택 정렬
        - 퀵 정렬 (얘는 안배움)
        - 삽입 정렬
        - 병합 정렬
- 안정성 vs 적응성
    - 안정성 : 동일한 키가 있을 때, 상대적 순서가 보존되는가?
    [1, 2, 3, **4**, **4**, 5, 6] → 동일한 4의 위치가 지켜져야함
    - 적응성 : 초기 정렬 상태 ⇒ 실행 시간이 바뀐다.
- 제자리 정렬
    - 외부 메모리를 쓰지 않고 정렬

### 버블 정렬

- 인접한 두개의 원소를 비교하며 자리를 계속 교환하는 방식
- 정렬 과정
    - 첫번째 원소부터 인접한 윈소끼리 자리를 교환하며 맨 마지막 자리까지 이동
    - 한 단계가 끝나면 가장 큰 원소가 마지막 자리로 정렬 (오름차순)
    - 교환하며 자리를 이동하는 모습이 물 위에 올라오는 거품 모양과 같다 하여 버블 정렬이라 부름
- 제자리 정렬 O
- 안정성 O
- 시간 복잡도 : O(n^2)

- 배열을 활용한 버블 정렬
    - 수도코드
    
    for i: N-1 → 1
    
    for j: 0 → i-1
    
    A[j] > A[j+1]
    
    A[j] ↔ A[j+1] 
    
    ```python
    def BubbleSort(a, N) : 	# 정렬할 List, N 원소 수
        for i in range(N-1, 0, -1) : # 범위의 끝 위치
            for j in range(i) :		# 비교할 왼쪽 원소 인덱스 j
                if a[j] > a[j+1] :
                    a[j], a[j+1] = a[j+1], a[j] 
    ```
    

### 카운팅 정렬

- 항목들의 순서를 결정하기 위해 집합에 각 항목이 몇 개씩 이는지 세는 작업을 하여, 선형 시간에 정렬하는 효율적인 알고리즘
- 비교기반 X N이 크고 K가 작을 때 유리 (N 변수 개수, K 범위)
- 제한 사항
    - 정수나 정수로표현 가능한 자료면 적용 가능
    - 집합 내 가장 큰 정수를 알아야 함
- 1단계
    - DATA에서 각 항목들의 발생 회수를 세고, 정수 항목들로 직접 인덱스 되는 카운트 배열 COUNTS에 저장
- 2단계
    - 정렬된 집합에서 각 항목의 앞에 위치할 항목의 개수를 반영하기 위해 COUNTS의 원소를 조정한다.
- 3단계
    - COUNTS[1]을 감소시키고 TEMP에 1을 삽입한다.
    - COUNTS[2}을 감소시키고 TEMP에 4를 삽입한다.
    - COUNTS[2]를 감소시키고 TEMP에 2를 삽입한다.
    - …

def Counting_Sort(DATA, TEMP, k):

# DATA[] — 입력 배열(원소는 0이상 k이하 정수)

# TEMP[] — 정렬된 배열

# COUNTS[] — 카운트 배열

COUNTS = [0]

### Baby-gin game

- 완전탐색
    
    : 문제의 해법으로 생각할 수 있는 모든 경우의 수를 나열해보고 확인
    
    - Brute-force 혹은 generate-and-test 기법이라 부름
    - 모든 경우의 수를 테스트한 후, 최종 해법을 도출
        - 일반적으로 경우의 수가 상대적으로 작을 때 유용하다.
    - baby-gin 접근
        - 고려할 수 이는 모든 경우의 수 생성
        - 해답 테스트
        - 순열
            
            e.g.
            
            for i1 in range(1, 4):
            
            for i2 in range(1, 4):
            
            if i2 !- i1:
            
            for i3 in range(1, 4):
            
            if i3 != i1 and i3 = i2:
            
            print(i1, i2, i3)
            
- 탐욕(Greedy) 알고리즘
    
    : 최적해를 구하는 데 사용되는 근시안적인 방법
    
    - 여러 경우 중 하나를 결정할 때 마다, 그 순간 최적이라 생각되는 것을 선택해 나가는 방식
    - 각 선택의 시점에서 이루어지는 결정은 지역적으로는 최적이지만, 최종적으로 최적이라는 보장은 없다.
    - 일반적으로, 머릿속에 떠오르는 생각을 검증 없이 바로 구현하면 Greedy 접근이 된다.
    - 동작 과정
        1. 해 선택 
        2. 실행 가능성 검사
        3. 해 검사
    
    e.g. 거스름돈 줄이기
    
    - 해 선택 : 단위가 큰 동전을 골라 거스름돈에 추가
    - 실행 가능성 검사 : 큰 동전의 합이 거스름돈보다 큰지 점검, 넘으면 최종적으로 추가한 동전을 뺌
    - 해 검사 : 금액이 일치하는지 확인, 모자라면 ‘해 선택’ 단계로 돌아가서 추가할 다른 동전으로 위 과정 진
- baby-gin 접근
    1. 숫자를 카운트
    2. run - triplet 순서대로 검증
    
    num = 45678 # Baby Gin 확인할 6자리 수
    
    c = [0]*12 # 6자리 수로부터 각 자리 수를 추출하여 개수를 누적할 리스트
    
    for i in range(6):
    
    c[num%10] += 1
    
    num//= 10
    
    ```python
    i = 0
    tri = run = 0
    while i < 10:
    	if c[i] >= 3: #triplete 조사 후 데이터 삭제
    		c[i] -= 3
    		tri += 1
    		continue;
    	if c[i] >= 1 and c[i+1] >= 1 and c[i+2] >= 1: #run 조사 후 데이터 삭제
    		c[i] -= 1
    		c[i+1] -=1 
    		c[i+2] -=1
    		run += 1
    		continue
    	i += 1
    	
    if run + tri == 2: print("Baby Gin")
    else: print("Lose")
    ```
    
    - 자주 실수하는 오답
        1. 단순 정렬 후, 앞뒤 3자리 끊어서 run 및 triplet을 확인