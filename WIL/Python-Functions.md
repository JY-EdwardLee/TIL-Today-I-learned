# Pyhon - Functions
## 함수

- 특정 작업을 수행하기 위한 재사용 가능한 코드 묶음
    - 왜 Why?
        
        : 코드의 중복을 방지
        
        : **재사용성**이 높아지고, **가독성**과 **유지보수성** 향상
        
    - 함수 호출 : function_name(arguments)
    - 함수 구조
        - parameter : 함수에 Input할 매개변수
        - function body
            
            └ document string : 설명서 (설명서 출력 시 “Docstring”에 있는 설명 확인 가능)
            
            └ return value : output으로 출력되는 값
            
        - 함수 정의
            
            ```python
            def + “함수 이름” + (파라미터) :
            	"""
            	Docstring (선택적)
            	"""
            	return + 반환할 값 ※ 필수 아님 (return 없을 시 None 반환 됨)
            	=> return에서 함수의 실행이 **종료** 됨
            ```
            
    - 함수와 **반환 값**
        - print() 함수는 반환 값이 없다.
    - 매개변수(parameter)와 인자(argument)
        - 매개변수
            
            : **함수를 정의할 때,** 함수가 받을 값을 나타내는 변수
            
        - 인자
            
            : **함수를 호출할 때,** 실제로 전달되는 값
            

### ★ **인자의 종류**

1. Positional Arguments (위치 인자)
    - 함수 호출 시 인자의 위치에 따라 전달되는 인자
        
        **※ 위치인자는 함수 호출 시 반드시 값을 전달해야 함**
        
2. Default Argument Values (기본 인자 값)
    - 함수 정의에서 매개변수에 **기본값**을 할당하는 것
    - 함수 호출 시 인자를 전달하지 않으면, **기본값이 매개변수에 할당**됨
3. Keyword Arguments (키워드 인자)
    - 함수 호출 시 인자의 이름과 함께 값을 전달하는 인자
    - 매개변수와 인자를 일치 시키지 않고, 특정 매개변수에 값을 할당할 수 있음
    - 인자의 순서는 중요하지 않으며, 인자의 이름을 명시하여 전달
        
        **※ 단, 호출 시 키워드 인자는 위치 인자 뒤에 위치해야 함 (앞에 올 수 없음)**
        
4. Arbitrary Argument Lists (임의의 인자 목록)
    - 정해지지 않은 개수의 인자를 처리하는 인자
    - 함수 정의 시 매개변수 앞에 ‘*’를 붙여 사용
    - 여러 개의 인자를 tuple로 처리
5. Arbitrary Keyword Argument Lists (임의의 키워드 인자 목록)
    - 정해지지 않은 개수의 키워드 인자를 처리하는 인자
    - 함수 정의 시 매개변수 앞에 ‘**’를 붙여 사용
    - 여러 개의 인자를 dictionary로 묶어 처리
- 함수 인자 권장 작성순서
    - 위치 → 기본 → 가변 → 가변 키워드
    - 호출 시 인자를 전달하는 가정에서 혼란을 줄일 수 있도록 함
        
        ※단, 모든 상황에 적용되는 절대적인 규칙은 아니며, 상황에 따라 유연하게 조정될 수 있음
        

### 재귀 함수(는 인셉션이다.)

- 함수 내부에서 자기 자신을 호출하는 함수
    
    e.g. 팩토리얼 구현 
    
- 함수 호출 시 **callstack**에 담기게 됨
- 특징
    - 특정 알고리즘 식을 표현할 때 변수의 사용이 줄고, 코드의 가독성이 높아짐
    - 1개 이상의 base case(종료되는 상황)가 존재하고, 수렴하도록 작성

★ 종료 조건을 명확히/반복되는 호출이 종료 조건을 향하도록

### 내장 함수

- 파이썬이 기본적으로 제공하는 함수 (별도의 import 없이 바로 사용 가능)
    
    ※ 외장 함수는 없음
    
- map & zip
    - **map(function, iterable)**
        
        : 순위 가능한 데이터구조(iterable)의 모든 요소에 함수를 적용하고, 그 결과를 **map objec**t로 반환
        
        - map object를 형 변환하여 확인 가능
    - **zip(*iterables)**
        
        : 임의의 iterable을 모아 **튜플**을 원소로 하는 **zip object**를 반환
        
        - zip object를 형 변환하여 확인 가능

### 함수와 Scope

- 함수는 코드 내부에 **local scope**를 생성하며, 그 외의 공간인 **global scope**로 구분
    - scope
        - global scope : 코드 어디에선든 참조할 수 있는 공간
        - local scope : 함수가 만든 scope (함수 내부에서만 참조 가능)
    - variable
        - global variable : global scope에 정의된 변수
        - local variable : local scope에 정의된 변수
- local scope에 존자해는 변수는 global scope에서 사용 불가
    
    ⇒ 변수의 **수명주기**와 관련 있음 (local에서 수명 주기가 끝남)
    
- 변수 수명주기
    1. built-in scope : 파이썬이 실행된 이후부터 영원히 유지
    2. global scope : 모듈이 호출된 시점 이후 혹은 인터프리터가 끝날 때까지 유지
    3. local scope : 함수가 호출될 때 생성, 함수가 종료될 때까지 유지
- 이름 검색 규칙 
    LEGB 룰 순서대로 찾아감
    
    **※ 함수 내에서 바깥 Scope의 변수에 접근 가능하나 수정은 불가능**
    
    - 정의한 변수 객체 삭제를 위해 ‘**del 변수**’를 입력 후 진행 가능
- global 키워드
    - 변수의 스코프를 전역 범위로 지정하기 위해 사용
    - 일반적으로 함수 내에서 전역 변수를 수정하려는 경우에 사용
    
    **※ global 키워드 선언 전에 참조 불가, 매개변수에는 global 키워드 사용 불가**
    

### 함수 스타일가이드

- 함수 명명 규칙
    - 기본 규칙
        1. 소문자와 언더스코어(_) 사용
        2. 동사로 시작하여 함수의 동작 설명
        3. 약어 사용 지양
    - 함수 이름 구성 요소
        - 동사 + 명사
        - 동사 + 형용사 + 명사
        - get/set 접두사 (얻기/세팅하기)
- 함수 설계 원칙
    
    <aside>
    💡
    
    **단일 책임 원칙**
    
    - 모든 객체는 하나의 명확한 목적과 책임만을 가져야 함
        - 만일 함수 내에 있는 여러 책임 중 하나만 이상이 생겨도 동작이 어렵고, 이슈를 찾기가 힘들어진다.
    </aside>
    

### Packing & Unpacking

- pakcing
    
    : 여러 개의 값을 하나의 변수에 묶어서 담는 것
    
    e.g.
    
    1. 한 변수에 콤마로 구분된 값을 넣으면 자동으로 튜플로 처리
    2. **‘*변수명’**을 사용하면 “나머지 모든 값”을 리스트로 묶어서 받을 수 있음
    3. **‘*매개변수’**를 사용하면 호출 시 여러 개의 인자를 한 변수에 묶어서 받을 수 있음
        
        (이 때 함수 내부에서 해당 매개변수는 튜플 형태로 취급)
        
- unpacking
    
    : 패킹된 변수를 풀어서 개별 변수나 함수 인자로 전달
    
    e.g.
    
    1. 튜플이나 리스트 등의 객체의 요소들을 개별 변수에 할당
    2. **‘*’**를 사용하면, 시퀀스(리스트, 튜플 등)를 함수에 전달할 때, 각 요소를 “풀어서” 개별 인자로 넘겨줄 수 있음
    3. ‘**’를 사용하면, 딕셔너리의 키-값 쌍을 분리해, 함수의 키워드 인자로 전달할 때 사용
- */** 연산자 정리
    - *
        
        : 패킹 연산자로 사용딜 때, 여러개의 인자를 하나의 리스트나 튜플로 묶음
        
        : 언패킹 연산자로 사용딜 때, 시퀀스나 반복 가능한 객체를 각각의 요소로 언패킹하여 함수의 인자로 전달
        
    - **
        
        : 언패킹 연산자로 사용될 때, 딕셔너리 키-값 쌍을 개별 키워드 인자로 전
        

### Lambda expressions

- 익명 함수를 만드는 데 사용되는 표현식 → 한 줄로 간단한 함수를 정의
    - 함수를 인자로 받을 때 활용 가능

---

## 복습

### deep_copy

- 문자열